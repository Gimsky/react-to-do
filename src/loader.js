import React from "react";

export default () => (
  <div>
    <div
      className="lds-dual-ring"
      style={{
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        margin: ".5rem",
      }}
    ></div>
  </div>
);
